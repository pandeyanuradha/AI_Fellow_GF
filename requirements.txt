# --- Core orchestration ---
pydantic>=2.5,<3
python-dotenv>=1.0
PyYAML>=6.0
typer>=0.12
rich>=13.7
jinja2>=3.1

# --- LLM clients ---
# OpenAI-compatible client — talks to Ollama (local, default), Groq, or the
# HF Inference router via a single base_url switch (see common/llm.py).
openai>=1.40
groq>=0.11

# --- Evaluator framework ---
deepeval>=1.0

# --- Bias / classification (fixes bug #5) ---
transformers>=4.41
torch>=2.2
sentencepiece>=0.2

# --- RAG ---
faiss-cpu>=1.8
sentence-transformers>=3.0
pypdf>=4.2

# --- Endpoint ---
fastapi>=0.115
uvicorn[standard]>=0.30
httpx>=0.27

# --- Tests ---
pytest>=8
